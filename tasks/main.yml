---

# (c) Wong Hoi Sing Edison <hswong3i@pantarei-design.com>
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

- name: include default variables
  include_vars: "../vars/main.yml"

- name: include release specific variables
  include_vars: "{{ lookup('first_found', _params) }}"
  vars:
    _params:
      files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths:
        - "../vars"
  ignore_errors: true

- name: include release specific tasks
  include_tasks: "{{ lookup('first_found', _params) }}"
  vars:
    _params:
      files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths:
        - "."
  ignore_errors: true

- name: prepare directories
  file:
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0755') }}"
    state: "{{ item.state | default('directory') }}"
  loop:
    - { dest: "/etc/bash_completion.d" }
    - { dest: "/usr/local/bin" }

- name: download archive
  shell: |
    set -euo pipefail
    TMP_DIR="$(mktemp -d)" \
      && cd $TMP_DIR \
      && mkdir -p ./{{ item.dest | dirname }} \
      && curl -skL -m 300 {{ item.url }} > ./{{ item.dest }} \
      && echo -n "{{ item.sha256sum }} ./{{ item.dest }}" | sha256sum -c - \
      && mkdir -p {{ item.dest | dirname }} \
      && mv ./{{ item.dest }} {{ item.dest }} \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "{{ item.dest | dirname }}"
  loop:
    - "{{ packer_download }}"
    - "{{ packer_plugin_vagrant_download }}"
    - "{{ packer_plugin_docker_download }}"
    - "{{ packer_plugin_qemu_download }}"
    - "{{ packer_plugin_virtualbox_download }}"
  register: result
  until: result is succeeded

- name: unarchive package
  shell: |
    set -euo pipefail
    exec &> >(tee -a {{ packer_download.dest }}.log)
    cd /usr/local/bin
    TMP_DIR="$(mktemp -d)" \
      && unzip -qq -d $TMP_DIR {{ packer_download.dest }} \
      && chown root:root $TMP_DIR/packer \
      && chmod 0755 $TMP_DIR/packer \
      && mv $TMP_DIR/packer . \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "{{ packer_download.dest }}.log"

- name: unarchive package
  shell: |
    set -euo pipefail
    exec &> >(tee -a {{ packer_plugin_vagrant_download.dest }}.log)
    cd /usr/local/bin
    TMP_DIR="$(mktemp -d)" \
      && unzip -qq -d $TMP_DIR {{ packer_plugin_vagrant_download.dest }} \
      && mv $TMP_DIR/packer-plugin-vagrant* $TMP_DIR/packer-plugin-vagrant \
      && chown root:root $TMP_DIR/packer-plugin-vagrant \
      && chmod 0755 $TMP_DIR/packer-plugin-vagrant \
      && mv $TMP_DIR/packer-plugin-vagrant . \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "{{ packer_plugin_vagrant_download.dest }}.log"

- name: unarchive package
  shell: |
    set -euo pipefail
    exec &> >(tee -a {{ packer_plugin_docker_download.dest }}.log)
    cd /usr/local/bin
    TMP_DIR="$(mktemp -d)" \
      && unzip -qq -d $TMP_DIR {{ packer_plugin_docker_download.dest }} \
      && mv $TMP_DIR/packer-plugin-docker* $TMP_DIR/packer-plugin-docker \
      && chown root:root $TMP_DIR/packer-plugin-docker \
      && chmod 0755 $TMP_DIR/packer-plugin-docker \
      && mv $TMP_DIR/packer-plugin-docker . \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "{{ packer_plugin_docker_download.dest }}.log"

- name: unarchive package
  shell: |
    set -euo pipefail
    exec &> >(tee -a {{ packer_plugin_qemu_download.dest }}.log)
    cd /usr/local/bin
    TMP_DIR="$(mktemp -d)" \
      && unzip -qq -d $TMP_DIR {{ packer_plugin_qemu_download.dest }} \
      && mv $TMP_DIR/packer-plugin-qemu* $TMP_DIR/packer-plugin-qemu \
      && chown root:root $TMP_DIR/packer-plugin-qemu \
      && chmod 0755 $TMP_DIR/packer-plugin-qemu \
      && mv $TMP_DIR/packer-plugin-qemu . \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "{{ packer_plugin_qemu_download.dest }}.log"

- name: unarchive package
  shell: |
    set -euo pipefail
    exec &> >(tee -a {{ packer_plugin_virtualbox_download.dest }}.log)
    cd /usr/local/bin
    TMP_DIR="$(mktemp -d)" \
      && unzip -qq -d $TMP_DIR {{ packer_plugin_virtualbox_download.dest }} \
      && mv $TMP_DIR/packer-plugin-virtualbox* $TMP_DIR/packer-plugin-virtualbox \
      && chown root:root $TMP_DIR/packer-plugin-virtualbox \
      && chmod 0755 $TMP_DIR/packer-plugin-virtualbox \
      && mv $TMP_DIR/packer-plugin-virtualbox . \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "{{ packer_plugin_virtualbox_download.dest }}.log"

- name: copy templates
  template:
    dest: "{{ item.dest }}"
    src: "{{ item.src | default('./templates' + item.dest + '.j2') }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
  loop:
    - { dest: "/etc/bash_completion.d/packer" }

- name: prepare files
  file:
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
    state: "{{ item.state | default('file') }}"
  loop:
    - { dest: "/etc/bash_completion.d/packer" }
    - { dest: "/usr/local/bin/packer", mode: "0755" }
